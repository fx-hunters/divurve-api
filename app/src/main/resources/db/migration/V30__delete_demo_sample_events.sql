-- 이슈 #191: V28 이 심은 시연용 예시 일정을 걷어낸다.
--
-- 왜 지금인가 — V28 은 자기 주석에 수명을 적어 두었다. "중앙은행 공식 일정 파서가 붙으면
-- (후속 이슈) 이 시드는 삭제한다". 이 이슈가 그 후속이며, CentralBankMeetingCatalog 가
-- FOMC·ECB·BOJ·금통위 일정을 공식 공표값으로 채운다.
--
-- 왜 남겨두면 안 되는가 — 시드의 날짜가 실제 공표 일정과 다르다. 예를 들어 FOMC 는 시드가
-- 2026-09-17, 실제는 2026-09-16 이다. 유니크 키가 (event_date, region, title) 이라 날짜가
-- 다르면 승격되지 않고 두 행이 나란히 남는다 — 같은 회의가 화면에 이틀로 보인다.
--
-- 범위 — DEMO_SAMPLE 전체다. V28 이 이 값을 쓴 유일한 경로이고, 이 서비스에 시연용 행을
-- 남길 이유가 더는 없다. AI_EXTRACTED 가짜 행은 V29(이슈 #184)가 이미 지웠다.
--
-- 되돌릴 일이 있으면 V28 을 다시 실행하면 된다 — 지우는 값이 그 파일에 그대로 적혀 있다.
delete from econ_events
 where source_kind = 'DEMO_SAMPLE';
